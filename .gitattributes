# 统一行尾：仓库内一律 LF。
#
# 背景：本仓库 core.autocrlf=false（未设 .gitattributes），索引里存的是 LF，
# 但工作区有一批文件被历史上的编辑工具写成了 CRLF —— 263 个 .go 里 240 个。
#
# 麻烦在于 git 的 stat 缓存（比对 mtime + size）不会去读文件内容，
# 所以 `git status` 一直显示干净，问题被掩盖。直到某次编辑真的碰到这些文件，
# git 才会读到真实内容，把**整个文件**判定为重写。
#
# 实际后果：2026-09-25 改 Go module 路径那次，diff 一度是 23738 行 ——
# 因为 122 个文件的行尾会被一并翻进仓库。那次是靠事后手工规范回 LF 才收干净。
#
# 显式声明 eol=lf 之后，checkout / checkin 都会强制 LF，
# 不再依赖每个人的本地 git 配置。
* text=auto eol=lf
